server:
  port: 8888

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/kkkooolllyyyaaa/config-service.git
          default-label: main
          force-pull: true
